```
PACKET ID: RPG-20260706-09
PROJECT:   Bali Life RPG
TARGET:    Codex
TITLE:     Rio's street race — the Act 1 setpiece that proves RIDE and starts the rivalry
PR TAG: [RPG-20260706-09]

===== BEGIN PACKET RPG-20260706-09 =====

ROLE & SCOPE
Phase 2 packet 3 (final). GAME_DESIGN.md Act 1 names a mid-act setpiece:
"street race vs Rio (pure RIDE)"; STORY_BIBLE.md §C gives Rio a public
streak-duel that sets the rivalry, and §I specs Rival Showdown as "[REUSE
delivery loop, new framing]... losing doesn't end the rivalry, it colors
it." Rio exists as an inert routine NPC with a default line only. This
packet ships the race and Rio's first authored beats.
HARD PREREQUISITES: RPG-20260706-03 (riding feel — a race on constant-
velocity movement proves nothing) and RPG-20260706-07 (letterbox/scripted-
walk primitives for staging). Do not start before both are merged.

HARD CONSTRAINTS
- Reuse-first: the race is the existing ride/checkpoint machinery under a
  shared timer against a scripted Rio ghost — Rio's scooter follows an
  authored route at a tuned pace (rubber-banded lightly so it's close for a
  first-timer, honest enough to lose). No new physics, no collision between
  racers.
- Trigger: authored one-time beat gated on Act 1 + >= 3 completed deliveries
  + driver rating >= 3.5, surfaced as a visible world scene at the scooter
  rental (Rio's turf) via the existing WorldScenes staging — NOT a phone
  ping. Player opts in through a short Rio dialogue (portrait from
  RPG-20260706-06 if landed).
- Both outcomes are authored content, per the Story Bible choice template:
  WIN -> small money side-bet payout, `+Relational` axis nudge, Rio memory
  `lost_to_you_clean`, one grudging-respect line. LOSE -> no money loss
  beyond the tiny stake, Rio memory `beat_you`, one needling line, and a
  rematch hook line. Fail-forward: losing never blocks progression, rating,
  or board access.
- Race state is transient (activeActivity-style, reusing the existing
  committed-activity persistence variant if interrupted — same pattern as
  `scooterRepair`). No new save-schema field; if unavoidable, stop and flag
  in the PR instead of bumping the schema.
- Race must be abandonable (ESC = concede with the LOSE outcome, clearly
  labeled before start: "Bail = he wins. He'll mention it.").

DELIVERABLES
1. `src/systems/ride/RivalRace.ts` (pure): route waypoint schedule, ghost
   pacing/rubber-band rules, outcome resolution from finish times.
2. Race staging in GameScene: letterbox count-in (3-2-1 via cutscene
   primitives), Rio ghost sprite on route, 3-4 route checkpoints reusing
   ride-checkpoint rendering, finish banner + outcome dialogue.
3. Rio authored content: challenge dialogue, win/lose reaction lines, one
   follow-up ambient line days later referencing the result (memory-driven,
   like Kadek's existing Rumah line). Written to STORY_BIBLE.md's Rio voice:
   cocky, sharp, weirdly likeable — never cartoon-hostile.
4. Axis/memory wiring through the existing AdjustReputationAxis intent and
   relationship-memory systems.

DEFINITION OF DONE
- `npm test -- --run` + `npm run build` pass; tests cover ghost pacing
  math, rubber-band bounds (ghost can never teleport-catch-up or run away
  beyond tuned caps), outcome resolution, both outcomes' axis/memory
  writes, and concede-on-ESC.
- firstHourProof path unchanged (race is optional content).
- PR includes a manual acceptance note: race played once to a win and once
  to a loss (godmode tuning shortcuts acceptable for setup, not for the
  race itself).
- STATE.md bullet + DECISIONS.md entry (first setpiece = product milestone).

DO NOT
- Do not build a generic race framework, leaderboards, or repeatable racing
  economy — ONE authored setpiece with a rematch HOOK (line only; the Act 3
  rematch is its own future packet).
- Do not let the race result gate any existing content.
- Do not add Rio relationship arcs/tiers beyond the memories + lines above.

===== END PACKET RPG-20260706-09 =====
```
